# ADR-003: Task 按异步语义建模

## 状态
Accepted

## 决策
`POST /api/v1/tasks` 返回任务壳，后续通过查询接口获取状态与结果。

## 原因
- LLM 调用与后处理存在长耗时
- 统一前后端状态流转模型
- 为后续 SSE / 队列扩展保留空间

## 影响
- 前端需基于状态轮询或 SSE 建立任务状态页
- 测试与审计要覆盖状态流转而不是只测同步返回
